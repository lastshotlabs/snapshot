/**
 * SVG inner content for Lucide icons.
 *
 * Each entry maps a kebab-case icon name to the SVG child elements
 * (path, circle, line, rect, polyline, etc.) that belong inside a
 * 24x24 `stroke="currentColor"` SVG container.
 *
 * Source: https://lucide.dev — MIT-licensed.
 */
export declare const ICON_PATHS: Record<string, string>;
