import { z } from "zod";
export declare const navUserMenuSlotNames: readonly ["root", "trigger", "triggerLabel", "triggerIcon", "avatar", "avatarImage", "email", "panel", "item", "itemLabel", "itemIcon", "separator", "label"];
export declare const navUserMenuConfigSchema: z.ZodType<Record<string, any>>;
