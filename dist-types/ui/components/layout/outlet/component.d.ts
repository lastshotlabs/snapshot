import type { OutletConfig } from "./types";
/**
 * Layout outlet primitive used to render nested child routes from the compiled
 * manifest route tree.
 */
export declare function Outlet({ config }: {
    config: OutletConfig;
}): import("react/jsx-runtime").JSX.Element | null;
