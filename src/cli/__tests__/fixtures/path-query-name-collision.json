{
  "openapi": "3.0.0",
  "info": { "title": "Path/query name collision", "version": "1.0.0" },
  "paths": {
    "/game/sessions/{sessionId}/players": {
      "get": {
        "tags": ["players"],
        "summary": "List players in a session",
        "parameters": [
          {
            "name": "sessionId",
            "in": "path",
            "required": true,
            "schema": { "type": "string" }
          },
          {
            "name": "sessionId",
            "in": "query",
            "required": false,
            "schema": { "type": "string" }
          },
          {
            "name": "connected",
            "in": "query",
            "required": false,
            "schema": { "type": "boolean" }
          }
        ],
        "responses": {
          "200": {
            "description": "OK",
            "content": {
              "application/json": {
                "schema": { "type": "array", "items": { "type": "string" } }
              }
            }
          }
        }
      }
    }
  }
}
