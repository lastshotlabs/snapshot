
/** Inferred config type from the List Zod schema. */
export type ListConfig = Record<string, unknown>;

/** Inferred type for a single static list item. */
export type ListItemConfig = Record<string, unknown>;
